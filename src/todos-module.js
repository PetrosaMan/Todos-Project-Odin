function renderTodo() {
  const todos = document.getElementById("todos");
  todos.innerHTML = ""; // Clear existing todos

  //let ptag = document.createElement("p");
  //ptag.classList.add("todo-description");
  //ptag.textContent = todo.description;
  //ptag.appendChild("todos");
}

export { renderTodo };
